import 'package:flutter/material.dart';

class AvatarColorScheme {
  final Color background;
  final Color foreground;

  const AvatarColorScheme({
    required this.background,
    required this.foreground,
  });
}